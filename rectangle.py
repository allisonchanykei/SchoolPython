#!/usr/bin/env python
w = float(raw_input('Enter the width of the rectangle(in m): '))
h = float(raw_input('Enter the base of the rectangle(in m): '))
a = w * h
print 'The area of the triangle is',a,'m^2'
